const parse = require('./../parse');

(async () => {
	// console.log('parse', parse);
	// console.log('parse.parse(\'$name = "mark"\')', await parse.parse('$name = "mark"'));
	// console.log('parse.parseHard($name = "mark")', parse.parseHard('$name = "mark"'));
})();
describe('parsering', () => {
	it('should work', () => {
		expect(1).toBe(1);
	});
});
